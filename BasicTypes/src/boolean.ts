let isDone: boolean = false;
isDone = true;
console.log(typeof isDone) // 'boolean'

let isOk: Boolean = true;
// let isNotOk: boolean = new Boolean(true); // 에러: boolean과 Boolean의 타입은 서로 다르다
console.log(typeof isOk); // 'boolean'

